---

base_locale: en
locales: [en]

data:
  external:
    - "<%= %x[bundle info decidim-core --path].chomp %>/config/locales/%{locale}.yml"

ignore_unused:
  - "decidim.features.cas_client.name"

ignore_missing:
  - decidim.participatory_processes.scopes.global
